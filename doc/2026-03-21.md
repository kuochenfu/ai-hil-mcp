# Dev Log — 2026-03-21

## Goals
- Port all MCP servers from Python to Rust for deployment simplicity (single static binary, no venv)
- Hardware-verify all Rust implementations against STM32WL55

## Completed

### Rust Track — All 3 MCP Servers Ported

#### `jtag-mcp-rs` (`probe-rs` + `rmcp`)
- Tools: `halt_cpu`, `resume_cpu`, `read_registers`, `read_memory`, `read_call_stack`, `diagnose_hardfault`
- Used `probe-rs` (Rust-native) instead of `pyocd` — more capable for STM32/nRF targets
- Binary size: ~10MB release
- **Hardware tested** on STM32WL55:
  - `halt_cpu` → PC = `0x08004102`
  - `read_registers` → all regs clean, Thread mode, SP in SRAM (`0x20003F8C`)
  - `read_memory(0x20000000, 32)` → valid SRAM data returned
  - `resume_cpu` → board running normally

#### `serial-mcp-rs` (`serialport` + `rmcp`)
- Tools: `list_serial_ports`, `read_serial_log`, `send_serial_command`
- Anomaly detection (HardFault, Panic, Watchdog) ported from Python
- **Hardware tested** on STM32WL55:
  - `list_serial_ports` → detected STLINK-V3 at `/dev/cu.usbmodem1303`
  - `read_serial_log` → captured live LoRa PING traffic (RSSI=-87/−88, SNR=12), no anomalies

#### `build-flash-mcp-rs` (`std::process::Command` + `rmcp`)
- Tools: `build_firmware`, `clean_build`, `get_build_size`, `flash_firmware`
- Supports CMakePresets.json and manual toolchain file, same as Python version
- **Hardware tested** full loop on STM32WL55 (`synapse-lora/CM4`):
  - `clean_build` → wiped `build/`
  - `build_firmware(preset="Debug")` → **SUCCESS** — `synapse-lora_CM4.elf` text=36072 data=168 bss=3152
  - `flash_firmware` → **SUCCESS** — verified and reset via ST-Link V3
  - `read_serial_log` → board alive, LoRa PING traffic confirmed

### MCP Config Updated
- `.mcp.json`: all 3 servers switched to Rust binaries
- Python versions kept as `*-py` fallback entries (`jtag-mcp-py`, `serial-mcp-py`, `build-flash-mcp-py`)
- `setup.sh`: auto-builds Rust binaries on first run; falls back to Python if `cargo` not available

### Toolchain Fix
- Root cause: Homebrew `arm-none-eabi-gcc` 15.2.0 no longer bundles newlib headers
- Fix: pinned `gcc-arm-none-eabi.cmake` to explicit ARM GNU Toolchain path at `/Applications/ArmGNUToolchain/15.2.rel1/arm-none-eabi/bin/`

## Summary

| MCP Server | Implementation | Status |
|------------|---------------|--------|
| `jtag-mcp-rs` | `probe-rs` + `rmcp` | ✅ HW tested |
| `serial-mcp-rs` | `serialport` + `rmcp` | ✅ HW tested |
| `build-flash-mcp-rs` | `std::process::Command` + `rmcp` | ✅ HW tested |

## Decisions
- **Rust selected for deployment simplicity** — single static binary, zero runtime dependencies (no venv/pip). Performance was secondary; I/O-bound tools don't benefit meaningfully from Rust's speed.
- **Python fallback kept** — `vision-mcp` (future) stays Python due to OpenCV binding complexity. All Python servers remain in `.mcp.json` as `*-py` entries.
- **`probe-rs` over `pyocd`** — better STM32/nRF support, actively maintained, first-class Rust citizen.

### Phase 3.1: Orchestrator — E2E Loop Verified (Simulator)

#### CLAUDE.md SOP
- Full 6-step Orchestrator SOP written into `CLAUDE.md`:
  1. Triage (anomaly classification table)
  2. Diagnosis (per-anomaly JTAG + register protocols)
  3. Remediation (fix source code)
  4. Build & Flash (`build_firmware` + `flash_firmware`)
  5. Verification (serial log confirms clean boot)
  6. Record (append to Known Bug Record)

#### `orchestrator/watcher.py`
- Polls serial port every 30s (configurable); detects anomaly keywords
- On detection: calls `claude --print "<SOP prompt>"` with anomaly lines embedded
- 180s cooldown prevents re-triggering during active remediation

#### `orchestrator/fault_sim.py`
- PTY-based virtual serial port simulator (macOS `pty.openpty()`)
- Streams realistic LoRa PING/PONG output, then injects configurable fault type
- Root cause of PTY fix: `tty.setraw(master_fd)` required so that when watcher opens slave with `serial.Serial()` (which calls `tcsetattr`), the line discipline doesn't disrupt data flow

#### `orchestrator/inject_fault.py`
- Writes fault message bytes to real serial port RX pin
- Limitation: writes go TO device (RX), not FROM device (TX) — watcher reads TX only; no loopback
- Useful for injecting from a second device; not for standalone simulation

#### E2E Test Result
```
[sim]     Injecting HardFault at t=5s → /dev/ttys001
[watcher] *** ANOMALY DETECTED at 07:36:49 ***
[watcher]   HardFault detected
[watcher] Triggering Claude Code orchestrator...
[claude]  Step 2A diagnosis initiated (JTAG tools requested)
```
- Full loop confirmed: simulator → watcher detects → claude invoked with SOP prompt → triage starts

## Summary

| Component | Status |
|-----------|--------|
| `jtag-mcp-rs` | ✅ HW tested |
| `serial-mcp-rs` | ✅ HW tested |
| `build-flash-mcp-rs` | ✅ HW tested |
| Orchestrator CLAUDE.md SOP | ✅ Written |
| `watcher.py` | ✅ Verified |
| `fault_sim.py` | ✅ E2E loop confirmed |

## Decisions
- **Rust selected for deployment simplicity** — single static binary, zero runtime dependencies. Performance secondary; I/O-bound tools don't benefit meaningfully from Rust speed.
- **Python fallback kept** — `vision-mcp` (future) stays Python due to OpenCV binding complexity.
- **`probe-rs` over `pyocd`** — better STM32/nRF support, actively maintained.
- **PTY over real serial for simulation** — `fault_sim.py` chosen over `inject_fault.py` after discovering serial writes go to device RX pin, not readable by watcher.

## Next
- Phase 2.3: PPK2 MCP server (pending hardware)
- Phase 2.4: Vision MCP server (pending webcam)
- Phase 3.2: Power Control MCP (USB relay for hard reset/power cycle)
